module.exports = 'select posts.id, posts."textContent", posts."createdAt", posts."imageUrl", users.username as author, count(likes."postId") as likes from followers inner join posts on followers."followingId" = posts."authorId" inner join users on users.id = posts."authorId" left join likes on likes."postId" = posts.id where followers."userId" = :uid group by (posts.id, users.username) order by "createdAt" desc';